const Node = function(value){
  this.value = value;
  this.next = null;
};

/**
 * 
 * @param {*} to Node
 * @param {*} from Node
 * @return Linked List Path of the Nodes
 */
const shortestPath = (to, from) => {
  if(!a || a === null || !b || b === null) {
    return;
  }
  else if(a === b) {
    return null; //the path is none
  }

};


